service: my-express-application


provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-2


functions:
  app:
    handler: src/index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'

plugins:
  - serverless-offline